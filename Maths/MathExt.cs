// Decompiled with JetBrains decompiler
// Type: RJ.Ext.Maths.MathExt
// Assembly: RJ, Version=2.0.0.0, Culture=neutral, PublicKeyToken=null
// MVID: 68AB702F-5ECA-40DF-86DF-FD9370761523
// Assembly location: C:\Users\WEB\Desktop\RJ.dll

using System;

namespace RX.Maths
{
  public static class MathExt
  {
    public static double Pow(this double x, double y)
    {
      return Math.Pow(x, y);
    }

    public static double Abs(this double x)
    {
      return Math.Abs(x);
    }

    public static double Acos(this double x)
    {
      return Math.Acos(x);
    }

    public static double Asin(this double x)
    {
      return Math.Asin(x);
    }

    public static double Atan(this double x)
    {
      return Math.Atan(x);
    }

    public static double Atan2(this double x, double y)
    {
      return Math.Atan2(x, y);
    }

    public static long BigMul(this int x, int y)
    {
      return Math.BigMul(x, y);
    }

    public static double Ceiling(this double x)
    {
      return Math.Ceiling(x);
    }

    public static Decimal Ceiling(this Decimal x)
    {
      return Math.Ceiling(x);
    }

    public static double Cos(this double x)
    {
      return Math.Cos(x);
    }

    public static double Cosh(this double x)
    {
      return Math.Cosh(x);
    }

    public static long DivRem(this long x, long y, out long z)
    {
      return Math.DivRem(x, y, out z);
    }

    public static int DivRem(this int x, int y, out int z)
    {
      return Math.DivRem(x, y, out z);
    }

    public static double Exp(this double x)
    {
      return Math.Exp(x);
    }

    public static double Floor(this double x)
    {
      return Math.Floor(x);
    }

    public static Decimal Floor(this Decimal x)
    {
      return Math.Floor(x);
    }

    public static double IEEERemainder(this double x, double y)
    {
      return Math.IEEERemainder(x, y);
    }

    public static double Log(this double x, double y)
    {
      return Math.Log(x, y);
    }

    public static double Log(this double x)
    {
      return Math.Log(x);
    }

    public static double Log10(this double x)
    {
      return Math.Log10(x);
    }

    public static double Max(this double x, double y)
    {
      return Math.Max(x, y);
    }

    public static byte Max(this byte x, byte y)
    {
      return Math.Max(x, y);
    }

    public static Decimal Max(this Decimal x, Decimal y)
    {
      return Math.Max(x, y);
    }

    public static float Max(this float x, float y)
    {
      return Math.Max(x, y);
    }

    public static int Max(this int x, int y)
    {
      return Math.Max(x, y);
    }

    public static long Max(this long x, long y)
    {
      return Math.Max(x, y);
    }

    public static sbyte Max(this sbyte x, sbyte y)
    {
      return Math.Max(x, y);
    }

    public static short Max(this short x, short y)
    {
      return Math.Max(x, y);
    }

    public static uint Max(this uint x, uint y)
    {
      return Math.Max(x, y);
    }

    public static ulong Max(this ulong x, ulong y)
    {
      return Math.Max(x, y);
    }

    public static ushort Max(this ushort x, ushort y)
    {
      return Math.Max(x, y);
    }

    public static double Min(this double x, double y)
    {
      return Math.Min(x, y);
    }

    public static byte Min(this byte x, byte y)
    {
      return Math.Min(x, y);
    }

    public static Decimal Min(this Decimal x, Decimal y)
    {
      return Math.Min(x, y);
    }

    public static float Min(this float x, float y)
    {
      return Math.Min(x, y);
    }

    public static int Min(this int x, int y)
    {
      return Math.Min(x, y);
    }

    public static long Min(this long x, long y)
    {
      return Math.Min(x, y);
    }

    public static sbyte Min(this sbyte x, sbyte y)
    {
      return Math.Min(x, y);
    }

    public static short Min(this short x, short y)
    {
      return Math.Min(x, y);
    }

    public static uint Min(this uint x, uint y)
    {
      return Math.Min(x, y);
    }

    public static ulong Min(this ulong x, ulong y)
    {
      return Math.Min(x, y);
    }

    public static ushort Min(this ushort x, ushort y)
    {
      return Math.Min(x, y);
    }

    public static double Round(this double x)
    {
      return Math.Round(x);
    }

    public static Decimal Round(this Decimal x)
    {
      return Math.Round(x);
    }

    public static Decimal Round(this Decimal x, int y)
    {
      return Math.Round(x, y);
    }

    public static Decimal Round(this Decimal x, MidpointRounding y)
    {
      return Math.Round(x, y);
    }

    public static double Round(this double x, int y)
    {
      return Math.Round(x, y);
    }

    public static double Round(this double x, MidpointRounding y)
    {
      return Math.Round(x, y);
    }

    public static double Round(this double x, int y, MidpointRounding z)
    {
      return Math.Round(x, y, z);
    }

    public static Decimal Round(this Decimal x, int y, MidpointRounding z)
    {
      return Math.Round(x, y, z);
    }

    public static int Sign(this Decimal x)
    {
      return Math.Sign(x);
    }

    public static int Sign(this double x)
    {
      return Math.Sign(x);
    }

    public static int Sign(this short x)
    {
      return Math.Sign(x);
    }

    public static int Sign(this long x)
    {
      return Math.Sign(x);
    }

    public static int Sign(this int x)
    {
      return Math.Sign(x);
    }

    public static int Sign(this float x)
    {
      return Math.Sign(x);
    }

    public static int Sign(this sbyte x)
    {
      return Math.Sign(x);
    }

    public static double Sin(this double x)
    {
      return Math.Sin(x);
    }

    public static double Sinh(this double x)
    {
      return Math.Sinh(x);
    }

    public static double Sqrt(this double x)
    {
      return Math.Sqrt(x);
    }

    public static double Tan(this double x)
    {
      return Math.Tan(x);
    }

    public static double Tanh(this double x)
    {
      return Math.Tanh(x);
    }

    public static double Truncate(this double x)
    {
      return Math.Truncate(x);
    }

    public static Decimal Truncate(this Decimal x)
    {
      return Math.Truncate(x);
    }
  }
}
